
const LoginPage = () => {
    // make an attractive design that our team selected, it like jsx code of reactJS. i just do rsc command in there. best of luck.
    
    return (
        <div>
            LoginPage
            <div className="mt-72">LoginPage</div>

            {/* when you would want to redirect or to go register page through link just use href="/register" in Link tag for routing */}
        </div>
    );
};

export default LoginPage;